TZ=UTC
PORT=3333
HOST=localhost
LOG_LEVEL=info
APP_KEY= # ask thomas for this!
NODE_ENV=development
DB_HOST=127.0.0.1 # this is currently setup for local dev with port forwarding and will change in the future
DB_PORT=5432
DB_USER=user
DB_PASSWORD= # fill in from kubernetes -> storage -> secrets.   then save this file and remove .example from the ending
DB_DATABASE=postgres